<h1>Listing nimbles</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @nimbles.each do |nimble| %>
  <tr>
    <td><%= nimble.content %></td>
    <td><%= nimble.user_id %></td>
    <td><%= link_to 'Show', nimble %></td>
    <td><%= link_to 'Edit', edit_nimble_path(nimble) %></td>
    <td><%= link_to 'Destroy', nimble, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Nimble', new_nimble_path %>
